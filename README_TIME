Цей код представляє собою простий таймер, який виконує дії після закінчення вказаного часу.

Код імпортує два модулі: time та winsound.

Модуль time використовується для роботи з часом в Python. Він надає функції для роботи з часом, такими як time.time(), яка повертає поточний час в секундах

Модуль winsound використовується для генерування звукових сигналів. Функція winsound.Beep(frequency, duration) генерує звуковий сигнал з вказаною частотою та тривалістю.

Два класи Timer та SmartTimer використовуються для створення та управління таймерами.

Клас Timer ініціалізується з трима параметрами: тривалістю та дійством, яке виконується після закінчення часу. Він має методи start, is_expired та execute_action. Метод start запускає таймер, is_expired перевіряє, чи закінчився час, а execute_action виконує дійство, якщо воно вказано.

Клас SmartTimer використовується для управління декількома таймерами. Він має методи add_timer, remove_timer та run. Метод add_timer додає таймер до списку таймерів, remove_timer видаляє таймер зі списку, а run запускає цикл, який перевіряє, чи закінчився час для кожного таймера, і виконує відповідне дійство, якщо час закінчився.

Функції beep та open_document використовуються як дії, які виконуються після закінчення часу. Функція beep генерує звуковий сигнал, а open_document має бути реалізована для відкриття документа.

Наприкінці коду створюються два таймери з різними тривалістями та дійствами, додаються до SmartTimer та запускаються.
